#include<stdio.h>

void XorShift32(unsigned int**, unsigned int, unsigned int );

void Durstenfeld(unsigned int**, int , unsigned int*);

int permut(char**, char*, unsigned int*, unsigned int );

int save_shared_memory(FILE*, int, int, char*);
